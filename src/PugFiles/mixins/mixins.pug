mixin skillbar(level)
    ul.mixin-list
        -for (var i=0; i < 12; i++)
            if(i < level )
                li.mixin-list__item.mixin-list__item_blue
            else
                li.mixin-list__item.mixin-list__item_gray
